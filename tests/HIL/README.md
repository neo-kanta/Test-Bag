# Hardware-in-the-loop (optional)

This folder intentionally contains scripts/instructions that run against physical devices.

Suggested local flow:
1. Flash `ESP32/ESPWIFI` firmware.
2. Run PC app with `--device=serial` once serial transport is configured.
3. Capture logs for regression triage.
