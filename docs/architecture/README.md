# Architecture

- `apps/pc-cli`: thin host.
- `libs/core`: protocol + domain logic.
- `libs/transport`: transport abstractions/implementations.
- `simulators/device-simulator`: fake device process.
- Legacy UI remains in `apps/pc-legacy-winforms`.
